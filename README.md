# Moved to github.com/Justichuu/u

This repository is an archived reflection of the protocol half of the U device.
Everything that lived here was consolidated into:

### → **[github.com/Justichuu/u](https://github.com/Justichuu/u)**

Use that repository. This copy is kept only so old links and clones still resolve
to an honest redirect, not an active tree.

Same device. Same faces (`1`, `0`, `u`). Same tape key `66cce8d50854`.
Provenance and the live protocol now live under `Justichuu/u`.
